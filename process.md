# تقرير مشاكل — جلسة اختبار الموبايل (Manual QA)

**التاريخ:** 2026-09-19
**البيئة:** Android Emulator (Pixel 7) + Flutter Web + Backend (Docker)

---

## 1. تسجيل الدخول والجلسة (Login / Session)

| # | المشكلة | الحالة | التفاصيل / السبب |
|---|---|---|---|
| 1.1 | شاشة Home بتعرض اسم ثابت "Alex" بدل اسم المستخدم الحقيقي (بالنص والـ avatar) | ✅ **تم الحل (موبايل)** | نص hardcoded بالكود، تم ربطه باسم المستخدم الحقيقي من `AuthProvider` |
| 1.2 | استرجاع الجلسة (Session Restore) ما كان بيجيب بيانات المستخدم الحقيقية بعد إعادة فتح التطبيق | ✅ **تم الحل (موبايل)** | تم ربط splash screen بـ `GET /api/users/me` الحقيقي بدل الاكتفاء بوجود token فقط |

---

## 2. البروفايل (Profile Screen)

| # | المشكلة | الحالة | التفاصيل / السبب |
|---|---|---|---|
| 2.1 | أرقام Trips / Saved / Countries كلها 0 دايمًا | ⚠️ **يحتاج إعادة اختبار** | كانت 404 لأنه الباك المحلي كان ناقص الـ endpoint أصلاً (`/api/users/me/stats`) — انحل بعد تحديث الباك، لازم تجربة من جديد بعد إنشاء رحلة ناجحة |
| 2.2 | العملة المفضلة والـ Pacing بتظهر بيانات وهمية ثابتة | ⚠️ **يحتاج إعادة اختبار** | نفس سبب 2.1 — endpoint الـ preferences ما كان موجود بالباك القديم |
| 2.3 | تعديل الاسم (Edit name) كان بيتحفظ محليًا بس على الجهاز، مش عالباك | ✅ **تم الحل (موبايل)** | تم ربطه بـ `PATCH /api/users/me` الحقيقي |

---

## 3. إنشاء رحلة (Trip Creation Wizard)

| # | المشكلة | الحالة | التفاصيل / السبب |
|---|---|---|---|
| 3.1 | إنشاء أي رحلة بيفشل بخطأ "We couldn't create your trip — AI generation failed validation after bounded retries" | ❌ **معلّق — محتاج من الباك — الأولوية القصوى** | السبب الجذري: **مفتاح Gemini API لسا placeholder** (`GEMINI_API_KEY=REPLACE_WITH...` بملف `.env`) — لازم مفتاح حقيقي من Google AI Studio |
| 3.2 | (سبب سابق كان موجود، انحل) جدول الأماكن (Places) بقاعدة البيانات كان فاضي بالكامل (0 صف) | ✅ **تم الحل (باك)** | تم تشغيل سكربت `seed-dataset.sql` يدويًا — الأماكن صارت موجودة (3 بالقدس، 2 بعمان)، بس هاد السكربت **مش مربوط تلقائيًا بالإقلاع** — أي مطوّر جديد رح يوقع بنفس المشكلة لحد ما ينحل |

---

## 4. عرض الرحلة (Trip Overview)

| # | المشكلة | الحالة | التفاصيل / السبب |
|---|---|---|---|
| 4.1 | الشاشة كانت دايمًا بتعرض بيانات وهمية ثابتة ("Tokyo") بغض النظر عن الرحلة الحقيقية | ✅ **تم الحل (موبايل)** | كانت الشاشة موصولة بـ `MockTripOverviewRepository` بدل الـ API الحقيقي — bug حقيقي بالكود، تم تصليحه |
| 4.2 | وصف المكان بالـ Place Detail Sheet ما كان يظهر (نص placeholder ثابت) | ✅ **تم الحل (موبايل)** | تم ربطه بـ `GET /api/places/{id}` الحقيقي |
| 4.3 | زر "Save" مو موجود إطلاقًا بشاشة Trip Overview | ✅ **تم الحل (موبايل)** | ميزة كانت ناقصة بالكامل من الواجهة رغم وجود الـ endpoint بالباك (`POST /api/trips/{id}/save`) — تم بناؤه |
| 4.4 | تعديل عنصر بالـ itinerary (Edit Item) كان بيتحفظ بالذاكرة بس، مش عالباك | ✅ **تم الحل (موبايل)** | تم ربطه بـ `PATCH /api/trips/{tripId}/itinerary/items/{itemId}` |
| 4.5 | "Regenerate" (يوم / عنصر) كانت بس رسالة "coming soon" | ⚠️ **مربوط بالكود، بس ما انختبر فعليًا** | تم ربطها بـ `POST /api/trips/{id}/generate` مع `scope=DAY/ITEM` — معتمدة على حل مشكلة 3.1 (Gemini key)، لازم إعادة اختبار بعدها |

---

## 5. الحفظ والأرشفة (Save / Archive)

| # | المشكلة | الحالة | التفاصيل / السبب |
|---|---|---|---|
| 5.1 | "Archive" مش شغال — برجع رسالة مضللة "التريب تغيّر بمكان تاني" | ✅ **تم إصلاح الرسالة (موبايل)** | السبب الحقيقي: الباك بيسمح بالأرشفة بس للرحلة بحالة **SAVED** (`Saved → Archived` فقط بالـ state machine). الرسالة هلق بتظهر السبب الصحيح من الباك |
| 5.2 | Save/Archive ما ينفذوا فعليًا لأي رحلة حاليًا | ❌ **معلّق — يعتمد على 3.1** | كل الرحلات الحالية عالقة بحالة DRAFT (لأنه التوليد بيفشل) — لازم التوليد ينجح الأول (يعني حل مشكلة Gemini) قبل ما نقدر نتحقق إنه Save/Archive شغالين فعليًا |
| 5.3 | ضغط متكرر وسريع على Archive برجع خطأ 429 "Too Many Requests" | ℹ️ **سلوك طبيعي — مش bug** | الباك فيه rate limit (10 طلبات/دقيقة لكل مسار) — لازم الانتظار شوي بين المحاولات، مو خطأ حقيقي |

---

## 6. نسخة الويب (Flutter Web)

| # | المشكلة | الحالة | التفاصيل / السبب |
|---|---|---|---|
| 6.1 | تسجيل الدخول وأي اتصال بالباك بيفشل برسالة "Could not reach the server" | ❌ **معلّق — محتاج من الباك** | مشكلة CORS: الباك مسموحله بس `http://localhost:5173`، وتطبيق الويب شغال حاليًا على بورت مختلف (`8765`). **حل مؤقت متاح:** تشغيل الويب على بورت `5173` مباشرة بدل ما ننتظر تعديل الباك |
| 6.2 | باقي الواجهة (splash, onboarding) شغالة بصريًا وبدون أخطاء JS | ✅ **تم التأكد** | لا مشاكل ملحوظة بالعرض نفسه، فقط بالاتصال بالباك |

---

## 7. البنية التحتية / إعداد الباك (اكتشفناها أثناء الاختبار، مش أخطاء بالواجهة)

| # | الملاحظة | الحالة |
|---|---|---|
| 7.1 | نسخة الباك المحلية كانت متأخرة 31 كوميت عن `origin/main` — endpoints كاملة (profile/preferences/stats/places/itinerary-patch) كانت غير موجودة أصلاً محليًا | ✅ تم الدمج |
| 7.2 | حاوية Docker (`triply-api`) كانت محتاجة إعادة بناء بعد الدمج عشان تاخد الكود الجديد | ✅ تم |
| 7.3 | سكربت زرع الأماكن (`seed-dataset.sql`) مش مربوط تلقائيًا — لازم يترن يدويًا كل مرة تنعمل قاعدة بيانات من جديد | ❌ يستاهل يتحول لخطوة تلقائية بالـ startup |
| 7.4 | مفتاح Gemini API لسا placeholder — موثّق أصلاً بـ `progress.md` تحت "Waiting For" | ❌ **الأولوية القصوى المتبقية** |

---

## الخلاصة — شو باقي عشان نقدر نكمل اختبار كامل؟

1. **مفتاح Gemini API حقيقي** — بدونه، ولا رحلة رح تكتمل توليدها، وبالتالي ولا Save ولا Archive ولا Regenerate ممكن نتأكد منهم فعليًا.
2. **إضافة `http://localhost:8765` (أو استخدام `5173`) لقائمة CORS** — عشان نقدر نختبر نسخة الويب كاملة.
3. **ربط `seed-dataset.sql` بعملية الإقلاع التلقائية** — عشان ما يتكرر نفس العطل مع أي مطوّر/بيئة جديدة.

بعد ما تنحل هاي الثلاثة، فيه إعادة اختبار كاملة لازم تصير لبنود 2.1, 2.2, 4.5, 5.2 (يلي معلقة على نجاح التوليد).
